spring:
  application:
    name: stock-service
    
server:
  port: 8301
eureka:
  client:
    #allow-redirects: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
  instance:
    hostname: localhost